export const BASE_URL =
  __ENV.BASE_URL || 'https://reqres.in';

// Default credentials (can be overridden by environment variables)

export const USERNAME =
  __ENV.USERNAME || 'eve.holt@reqres.in';

export const PASSWORD =
  __ENV.PASSWORD || 'cityslicka';

// API paths (can be overridden by environment variables)

export const PATH =
  __ENV.PATH || 'api/login';

export const POST_PATH =
  __ENV.POST_PATH || 'api/posts';